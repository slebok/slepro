[
  language(bgl(text), elementOfBgl, []),
  language(bgl(term), eslLanguage, ['as.term']),

  parser(bgl(text), bgl(term), parseBgl, []),

  elementOf('as.esl', esl(text)),
  elementOf('as.term', esl(term))
].

